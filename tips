python manage.py startapp polls  to create new app


activate virtual environment:
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt


create new app 
python manage.py startapp myapp

fdfdf


to revert pull:
1.git log
3. Discard all changes in the working directory: git reset --hard HEAD
4. Alternatively, if you want to keep the history and revert the merge commit:
   git revert -m 1 1234567

   